const TitleCart = ({ title }: { title: string }) => {
   return (
      <p className="font-sans text-[14px] font-[500] capitalize leading-[20px]">
         {title}
      </p>
   )
}

export default TitleCart

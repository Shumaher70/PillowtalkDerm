const TitleCart = ({ title }: { title: string }) => {
   return (
      <p className="text-[14px] leading-[20px] font-[500] font-sans capitalize">
         {title}
      </p>
   );
};

export default TitleCart;
